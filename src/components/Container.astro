---
import { cn } from "../../lib/utils";

const { class:className } = Astro.props;
const {id} = Astro.props

---
<div class={cn("mx-auto max-w-7xl px-4 sm:px-6 lg:px-8", className)} id={id}>
    <slot />
</div>